<div class="navigation-links">
    <nav><a class="link" ui-sref="profile" title="{{profile.name || profile.email}}">
        Profile <span ng-bind="'[' + (profile.name || profile.email) + ']'"></span>
    </a></nav>
    <nav><a class="link" ui-sref="board">Board</a></nav>
    <nav><a class="link" ng-click="signOut()">Sign Out</a></nav>
</div>